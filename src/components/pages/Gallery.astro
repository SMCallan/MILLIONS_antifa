---
import { ImageOff, Upload } from "lucide-react";

import { SignalFieldHero } from "@/components/SignalFieldHero";
import { buttonVariants } from "@/components/ui/button";
import { getLangFromUrl, localizePath, useTranslations } from "@/i18n";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

// Placeholder tiles until real works are confirmed for the tour.
const tiles = [0, 1, 2, 3, 4, 5];
---

<SignalFieldHero
  client:load
  compact
  headline={t.gallery.heroTitle}
  subtitle={t.gallery.heroSubtitle}
  primaryCta={{ label: t.gallery.contributeCta, href: localizePath("/contribute", lang) }}
  secondaryCta={{ label: t.hero.primaryCta, href: localizePath("/tour-dates", lang) }}
/>

<section class="section-y bg-background">
  <div class="container-page">
    <div class="mb-10 max-w-2xl">
      <h2 class="text-3xl font-black leading-tight md:text-4xl">{t.gallery.placeholderTitle}</h2>
      <p class="mt-4 text-base leading-8 text-muted-foreground">{t.gallery.placeholderBody}</p>
    </div>

    <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
      {
        tiles.map((tile) => (
          <div
            class:list={[
              "poster-field-fallback group relative grid place-items-center overflow-hidden border-2 border-foreground bg-foreground shadow-[0.3rem_0.3rem_0_var(--foreground)]",
              tile === 0 || tile === 4 ? "aspect-square sm:aspect-[4/3]" : "aspect-[4/3]",
            ]}
          >
            <ImageOff className="relative z-10 size-7 text-white/35 transition-transform group-hover:scale-110" aria-hidden="true" />
            <span class="absolute left-3 top-3 z-10 text-[0.6rem] font-bold uppercase tracking-[0.14em] text-white/55">
              MWAF / {String(tile + 1).padStart(2, "0")}
            </span>
            <span class="absolute bottom-3 right-3 z-10 border border-white/30 bg-black/35 px-2 py-1 text-[0.58rem] font-bold uppercase tracking-[0.12em] text-white/55 backdrop-blur-sm">
              {t.common.comingSoon}
            </span>
          </div>
        ))
      }
    </div>

    <div class="mt-10">
      <a class={buttonVariants({ size: "lg" })} href={localizePath("/contribute", lang)}>
        <Upload className="size-4" aria-hidden="true" />
        {t.gallery.contributeCta}
      </a>
    </div>
  </div>
</section>
